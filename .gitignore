# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
noteflow
noteflow-go
noteflow.exe

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Go workspace file
go.work

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Local test files. notes.md is ignored everywhere by default (it's
# a per-project user file), but this repo's own root notes.md doubles
# as a demo of what NoteFlow can render — see the file. The negation
# below un-ignores ONLY the root notes.md.
notes.md
!/notes.md
assets/
*.db

# Test folders
test-folder/
test-folder*/

# Config files (user-specific)
noteflow.json

# Temporary files
/tmp/
*.log

# Homebrew tap (separate repository)
homebrew-tap/

# GoReleaser dist directory
dist/
